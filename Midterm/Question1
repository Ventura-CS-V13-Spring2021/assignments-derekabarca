#include <iostream>
#include <fstream>
#include <string>
using namespace std;

int main() {

    string stdName;
    int twoScores;

    ifstream inFile;
    inFile.open("students.txt");
    if (inFile.fail()){
        cerr << "An error has ocurred opening the file " << endl;
        exit (0);
    }

    inFile >> stdName;
    inFile >> twoScores;
    
    //got stuck on the code, I know what I am supposed to do, but not sure how to do it, and I do not want to cheat.
    
    return 0;
}