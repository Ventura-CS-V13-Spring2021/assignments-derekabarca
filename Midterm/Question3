#include <iostream>
#include <cstdlib>
#include <ctime>
#include <fstream>


int getRdnum ()
{
    srand(time(0));

    for (int x = 1; x <= 3; x++){
        cout << 1 + (rand() % 9)
    }
}

using namespace std;


int main() {
    return 0;
}