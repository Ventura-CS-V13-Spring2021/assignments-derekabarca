#include <iostream>
#include <fstream>
#include <string>
using namespace std;

int main() {

    string stdName;
    int twoScores;

    ifstream inFile;
    inFile.open("students.txt");
    if (inFile.fail()){
        cerr << "An error has ocurred opening the file " << endl;
        exit (0);
    }

    inFile >> stdName;
    inFile >> twoScores;
    //getline(inFile, stdName);
    //getline(inFile, twoScores);
    //cout << stdName << endl << twoScores << endl;
    

    return 0;
}