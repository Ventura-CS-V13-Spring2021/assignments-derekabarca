#include <iostream>
#include <cstdlib>
#include <ctime>
#include <fstream>


int getRdnum ();
{
    int num1, num2, num3;
    srand(time(0));

    for (int x = 1; x <= 3; x++){
        cout << 1 + (rand() % 9) << endl;
        //cin >> num1, num2, num3;
    }
}

int findMin(num1, num2, num3);{
    if (num1 < num2 && num1 < num3){
        cout << num1;
    }
    if (num2 < num1 && num2 < num3);
    cout << num2;
}



void fileWrite (ofstream &, int);

using namespace std;


int main() {

    cout << getRdnum();
    return 0;
}